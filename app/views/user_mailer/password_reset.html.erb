<h1>UserMailer#password_reset</h1>

<p>
  <%= @greeting %>, to reset your password follow link:
  <%= link_to 'reset password', edit_password_reset_url(@user.reset_digest, email: @user.email) if @user.reset_digest %>

  This link will expire in two hours.
</p>
