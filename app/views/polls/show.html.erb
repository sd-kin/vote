<%= render @poll %>
<div id='choice'>
  <% if @already_voted %>
    <h2> Your choice already accepted </h2>
  <% end %>
  <%= button_to 'make choice', "javascript:makeChoice()" unless @already_voted %>
</div>
<%= link_to 'back', :back %>
