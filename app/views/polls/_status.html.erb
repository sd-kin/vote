<%= button_to 'Make ready', make_ready_poll_path(poll), { class: "btn btn-primary btn-block", remote: true } if poll.able_to_ready? %>
<%= button_to 'Make draft', make_draft_poll_path(poll), remote: true, data: { confirm: 'Votation progress will be lost!' } if poll.able_to_draft? %>
  (All progress will be lost!)
