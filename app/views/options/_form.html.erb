<div id = "<%= dom_id(Option.new, :errors_for_new) %>">
</div>

<div id = "<%= dom_id(Option.new) %>">
  <%= form_for Option.new, url: poll_options_path(poll), html: {method: :post}, remote: true do |f| %>
    <%= f.label 'Title:'%>
    <%= f.text_field :title %> <br>
    <%= f.label 'Description:'%>
    <%= f.text_field :description %> <br>
    <%= f.submit 'Add' %>
  <% end %>
</div>