<div id="<%= dom_id(option) %>">
  <div id="<%= dom_id(option, :errors_for) %>">
  </div>

  <li>
    Title: <%= option.title %> <br>
    Description: <%= option.description %> <br>
    <%= link_to 'Edit', edit_poll_option_path(option.poll, option), remote: true %> 
    <%= link_to 'Delete', poll_option_path(option.poll, option), method: :delete, remote: true %>
  </li>
</div>